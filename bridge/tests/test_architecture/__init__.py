"""Architecture guard tests.

These tests enforce structural invariants of the codebase that prevent
regressions to known anti-patterns. They scan source files for forbidden
import patterns rather than testing runtime behavior.
"""
