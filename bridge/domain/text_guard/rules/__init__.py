"""YAML rule files for Text Guard.

These files are the human-readable source format for language rules.
They are loaded by the application layer (text_guard_service.py)
and converted to domain RuleSet objects.

The domain layer itself uses Python data structures from rules_registry.py.
"""
