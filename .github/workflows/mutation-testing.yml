name: Mutation Testing

on:
  schedule:
    - cron: '0 6 * * 0'  # Sunday 06:00 UTC weekly
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mutate-language:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'
      - name: Install dependencies
        working-directory: ./bridge
        run: |
          pip install -e ".[test]"
          pip install "mutmut<3"
      - name: Mutate application/language/
        working-directory: ./bridge
        run: |
          mutmut run \
            --paths-to-mutate "application/language/" \
            --tests-dir "tests/" \
            --runner "python -m pytest tests/test_application/test_language/ -x -q --tb=no" \
            || true
          mutmut results
        continue-on-error: true
      - name: Upload mutation cache
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: mutation-cache-language
          path: bridge/.mutmut-cache/

  mutate-stream-guard:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'
      - name: Install dependencies
        working-directory: ./bridge
        run: |
          pip install -e ".[test]"
          pip install "mutmut<3"
      - name: Mutate stream_guard.py
        working-directory: ./bridge
        run: |
          mutmut run \
            --paths-to-mutate "application/language/stream_guard.py" \
            --tests-dir "tests/" \
            --runner "python -m pytest tests/test_application/test_language/test_stream_guard.py tests/test_application/test_language/test_stream_guard_hypothesis.py -x -q --tb=no" \
            || true
          mutmut results
        continue-on-error: true
      - name: Upload mutation cache
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: mutation-cache-stream-guard
          path: bridge/.mutmut-cache/

  mutate-sentry:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'
      - name: Install dependencies
        working-directory: ./bridge
        run: |
          pip install -e ".[test]"
          pip install "mutmut<3"
      - name: Mutate main.py (sentry filter)
        working-directory: ./bridge
        run: |
          mutmut run \
            --paths-to-mutate "main.py" \
            --tests-dir "tests/" \
            --runner "python -m pytest tests/test_infrastructure/test_sentry_integration.py tests/test_adversarial/test_K8_sentry_pii.py -x -q --tb=no" \
            || true
          mutmut results
        continue-on-error: true
      - name: Upload mutation cache
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: mutation-cache-sentry
          path: bridge/.mutmut-cache/

  mutate-privacy:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'
      - name: Install dependencies
        working-directory: ./bridge
        run: |
          pip install -e ".[test]"
          pip install "mutmut<3"
      - name: Mutate skill_compression/privacy/
        working-directory: ./bridge
        run: |
          mutmut run \
            --paths-to-mutate "application/skill_compression/privacy/" \
            --tests-dir "tests/" \
            --runner "python -m pytest tests/test_application/test_skill_compression/test_privacy_pipeline.py tests/test_application/test_skill_compression/test_privacy_healthcare.py tests/test_application/test_skill_compression/test_privacy_nudge.py tests/test_application/test_skill_compression/test_privacy_secrets.py tests/test_adversarial/test_K6_privacy_bypass.py -x -q --tb=no" \
            || true
          mutmut results
        continue-on-error: true
      - name: Upload mutation cache
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: mutation-cache-privacy
          path: bridge/.mutmut-cache/

  mutate-rate-limiter:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'
      - name: Install dependencies
        working-directory: ./bridge
        run: |
          pip install -e ".[test]"
          pip install "mutmut<3"
      - name: Mutate rate_limiter.py
        working-directory: ./bridge
        run: |
          mutmut run \
            --paths-to-mutate "application/rate_limiter.py" \
            --tests-dir "tests/" \
            --runner "python -m pytest tests/test_application/test_rate_limiter.py tests/test_application/test_rate_limiter_benchmark.py -x -q --tb=no" \
            || true
          mutmut results
        continue-on-error: true
      - name: Upload mutation cache
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: mutation-cache-rate-limiter
          path: bridge/.mutmut-cache/
